
<link rel="stylesheet" type="text/css" href="all.css" />
<h1>안녕하세요{{#each userinfo}}<span> {{c_name}}</span>{{/each}} 고객님, 예약할 차량과 날짜를 선택해주십시오.</h1>
<h1>{{title1}}</h1>
    <table>
        <div class="availablecar"> <tr>
            <td>차대번호</td>
            <td>모델명</td>
            <td>색상</td>
            <td>생산연도</td>
            <td>가격($)</td>
            <td>분류</td>
            <td>예약날짜</td>
            <td>예약</td>
        </tr></class>
   
        {{#each availablecars}}
        <tr>
            <td>{{vin}}</td>
            <td>{{model}}</td>
            <td>{{{color}}}</td>
            <td>{{productionyear}}</td>
            <td>{{price}}</td>
            <td>{{category}}</td>
            
             <form method="post" action="/reserve/{{r_vin}}">
                <td>
                <input type="date" name="reservedate" required="required" >
                </td>
                <td> 
                <button name="reserve"  value="{{vin}}" formaction="/reserve/{{vin}}">예약</button>
                </td>
            </form>
        </tr>
        {{/each}}
        </div>
    </table>
    <table>
        <div class="reservecar" style="margin-top: 5%;">
            <h1>{{#each userinfo}}<span> {{c_name}}</span>{{/each}} 고객님께서, 
            예약하신 차량내역입니다.</h1>
            <span style="color:red;">수정하시려면 예약 취소후 재예약 부탁드립니다.</span>
             <tr>
            <td>예약번호</td>
            <td>차대번호</td>
            <td>예약일자</td>
            <td>차량정보</td>
            <td>예약</td>
            </tr></class>
            </div>
            {{#each reservelist}}
            <tr>
            <td>{{reservenum}}</td>
            <td>{{{r_vin}}}</td>
            <td>{{r_date}}</td>
            <form method="get" action="/detail">
                <td>
                <button name ="detail" value="{{r_vin}}" formaction="/detail/{{r_vin}}">상세정보</button>
                </td>
            </form>

            <form method="post" action="/reserve/delete">
                <td>
                <button name='delete' value="{{r_vin}}"formaction="/reserve/delete/{{r_vin}}">취소하기</button>
                </td>
            </form>
            
            </tr>
            {{/each}}
    </table>
